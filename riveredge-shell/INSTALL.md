# RiverEdge Shell - 安装指南

## 📋 前置要求

- Node.js >= 18.0.0
- npm >= 9.0.0

## 🚀 安装步骤

### 1. 安装依赖

```bash
cd riveredge-shell
npm install
```

### 2. 启动开发服务器

```bash
npm run dev
```

### 3. 访问应用

打开浏览器访问：http://localhost:8000

## 📦 已安装的核心依赖

- **@umijs/max**: Umi V4 应用框架（包含所有插件）
- **antd**: Ant Design v6 UI 组件库
- **@ant-design/pro-components**: Ant Design Pro 高级组件
- **@ant-design/icons**: Ant Design 图标库
- **@ant-design/charts**: Ant Design 图表库
- **@formily/core + @formily/react + @formily/antd-v5**: Formily 表单库
- **react-to-print + jspdf + html2canvas**: 打印功能库
- **dayjs**: 日期处理库
- **lodash-es**: 工具函数库

## 🔧 项目结构

```
riveredge-shell/
├── src/
│   ├── pages/              # 页面目录
│   │   ├── login/          # 登录页面
│   │   ├── dashboard/      # 仪表盘页面
│   │   └── 404.tsx         # 404 页面
│   ├── layouts/            # 布局组件
│   │   └── BasicLayout.tsx # 基础布局
│   ├── components/         # 公共组件
│   ├── services/           # API 服务
│   │   ├── api.ts          # API 基础配置
│   │   ├── auth.ts         # 认证 API
│   │   └── user.ts         # 用户 API
│   ├── models/             # Umi Model (数据流)
│   │   ├── user.ts         # 用户状态管理
│   │   └── global.ts       # 全局状态管理
│   ├── utils/              # 工具函数
│   │   ├── auth.ts         # 认证工具
│   │   ├── storage.ts      # 存储工具
│   │   └── format.ts       # 格式化工具
│   ├── types/              # TypeScript 类型定义
│   │   └── api.ts          # API 类型
│   ├── locales/            # 国际化文件
│   │   ├── zh-CN.ts        # 中文
│   │   └── en-US.ts        # 英文
│   ├── access.ts           # 权限定义
│   ├── app.tsx             # 应用入口
│   └── global.less         # 全局样式
├── public/                 # 静态资源
├── .umirc.ts               # Umi 配置文件
├── tsconfig.json           # TypeScript 配置
└── package.json
```

## ⚠️ 注意事项

1. **后端服务**: 确保后端服务（riveredge-core）已启动，默认运行在 http://localhost:8000
2. **代理配置**: 开发环境的 API 代理配置在 `.umirc.ts` 中，生产环境需要配置 Nginx 等反向代理
3. **环境变量**: 如需配置环境变量，创建 `.env` 文件

## 📚 相关文档

详细开发计划和规范请参考 `Farming Plan/` 目录。


import type { IFilterValues, IPrintConfig, IRenderColumn, IRenderHeaderCell, TColumnType } from "./types";
export declare function getPrintColumns(columns: IRenderColumn[], config: IPrintConfig): IRenderColumn[][];
export declare function getHeaderFooterPrintColumns(columns: IRenderColumn[], type: TColumnType, rowsHeights: number[]): IRenderHeaderCell[][];
export declare function normalizePrintConfig(config?: IPrintConfig): {
    mode: "portrait" | "landscape";
    ppi: number;
    paper: "a4" | "a3" | "letter";
};
export declare function getPrintCellStyle(cell: IRenderHeaderCell, columnWidth: number): string;
export declare function getPrintFilterValue(filterValues: IFilterValues, columns: IRenderColumn[], cell: IRenderHeaderCell): any;

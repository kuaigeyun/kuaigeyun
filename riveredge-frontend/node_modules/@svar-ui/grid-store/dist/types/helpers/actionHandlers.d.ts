import { TID } from "@wx/lib-state";
import type { IApi } from "../types";
export declare function handleAction(api: IApi & {
    _temp: {
        id: TID;
        cut: boolean;
    }[];
}, action: string): void;

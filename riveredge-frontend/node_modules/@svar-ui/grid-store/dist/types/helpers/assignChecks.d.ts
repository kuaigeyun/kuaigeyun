import type { TID } from "@wx/lib-state";
import type { IApi, IHistory } from "../types";
export interface ICheckableItem {
    id?: TID;
    isDisabled?: ((ids: TID[], data: any[]) => boolean) | ((history: IHistory) => boolean) | ((api: IApi & {
        _temp: {
            id: TID;
            cut: boolean;
        }[];
    }) => boolean);
    [key: string]: any;
}
export declare function assignChecks<T extends ICheckableItem>(items: T[]): T[];

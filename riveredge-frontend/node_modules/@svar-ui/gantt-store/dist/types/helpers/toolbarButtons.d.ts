import type { IData, IDataHash, IParsedTask, IHistory, IDataConfig } from "../types";
import type { TID } from "@svar-ui/lib-state";
export declare function getToolbarButtons(config?: IDataConfig): IButtonConfig[];
export interface IButtonConfig {
    id?: string;
    comp: string;
    text?: string;
    icon?: string;
    type?: string;
    menuText?: string;
    isDisabled?: ((task: IParsedTask, state: IData, target?: TID | IDataHash) => boolean) | ((history: IHistory) => boolean);
    isHidden?: (task: IParsedTask, state: IData, target?: TID | IDataHash) => boolean;
}
export declare const defaultToolbarButtons: IButtonConfig[];

import { Metadata } from '@grpc/grpc-js';
import { Observable } from 'rxjs';
import { Reward } from '../../bizdomain/common';
import { RedemptionCodeStatus } from '../../bizdomain/redemption_code';
import { Error } from '../../univer/constants/errors';
export declare const protobufPackage = "universer.v1";
/** http POST /universer-api/promotion/redemption-code/redeem */
export interface RedeemRequest {
    redemptionCode: string;
}
export interface RedeemResponse {
    rewards: Reward[];
    error: Error | undefined;
}
/** http GET /universer-api/promotion/redemption-code/get-by-code */
export interface GetRedemptionCodeByCodeRequest {
    code: string;
}
export interface GetRedemptionCodeByCodeResponse {
    redemptionCode: RedemptionCode | undefined;
    error: Error | undefined;
}
export interface RedemptionCode {
    status: RedemptionCodeStatus;
    beginTime: string;
    endTime: string;
    rewards: Reward[];
}
export interface RedemptionCodeService {
    Redeem(request: RedeemRequest, metadata?: Metadata): Observable<RedeemResponse>;
    GetRedemptionCodeByCode(request: GetRedemptionCodeByCodeRequest, metadata?: Metadata): Observable<GetRedemptionCodeByCodeResponse>;
}

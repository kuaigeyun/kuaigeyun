import { ContextMenu } from '@ant-design/pro-editor';
import { memo } from 'react';
import { useMenuActions } from "./useMenuAction";
import { jsx as _jsx } from "react/jsx-runtime";
var RightClickContextMenu = /*#__PURE__*/memo(function () {
  var actions = useMenuActions();
  var defaultMode = [
  // 默认模式
  actions.paste, actions.DIVIDER, actions.selectAll, actions.zoomIn, actions.zoomOut];

  // TODO：选中一个节点

  // TODO：选中一条边

  // TODO：选中一组节点

  return /*#__PURE__*/_jsx(ContextMenu, {
    items: defaultMode
  });
});
export default RightClickContextMenu;
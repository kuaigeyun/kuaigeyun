import { FC } from 'react';
import { FlowEditorStore } from '../../store';
import { CommonUpdaterProps } from './Common';
export interface StoreUpdaterProps extends Omit<CommonUpdaterProps, 'useStateUpdater'>, Pick<FlowEditorStore, 'onViewPortChange'> {
}
declare const StoreUpdater: FC<StoreUpdaterProps>;
export default StoreUpdater;

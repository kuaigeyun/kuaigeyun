import React from 'react';
import { Language, MiniMapPosition } from '../constants';
interface MiniMapProps {
    visible?: boolean;
    className?: string;
    position?: MiniMapPosition;
    language?: Language;
}
declare const MiniMap: React.FC<Partial<MiniMapProps>>;
export default MiniMap;

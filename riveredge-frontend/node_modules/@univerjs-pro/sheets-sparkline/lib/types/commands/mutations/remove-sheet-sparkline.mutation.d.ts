import { IMutation, IRange } from '@univerjs/core';
interface IRemoveSheetSparklineMutationProps {
    unitId: string;
    subUnitId: string;
    ranges?: IRange[];
    isSingle: boolean;
    groupIds?: string[];
}
export declare const RemoveSheetSparklineMutation: IMutation<IRemoveSheetSparklineMutationProps>;
export {};

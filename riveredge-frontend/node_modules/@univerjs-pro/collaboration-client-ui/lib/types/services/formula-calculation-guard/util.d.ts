import { ISetFormulaCalculationNotificationMutation } from '@univerjs/engine-formula';
/**
 * Determine if the calculation is starting based on params
 * @param params
 * @returns
 */
export declare function isCalculationStart(params: ISetFormulaCalculationNotificationMutation): boolean;
/**
 * Determine if the calculation has completed based on params
 */
export declare function isCalculationComplete(params: ISetFormulaCalculationNotificationMutation): boolean;

import { ICommand } from '@univerjs/core';
export interface ISetPivotFilterCommandParams {
    /**
     * @property {string} unitId  The unit id of workbook which the pivot table belongs to.
     */
    unitId: string;
    /**
     * @property {string} subUnitId  The sub unit id of worksheet which the pivot table belongs to.
     */
    subUnitId: string;
    /**
     * @property {string} pivotTableId  The pivot table id.
     */
    pivotTableId: string;
    /**
     * @property {string} tableFieldId  The table field id. Which is the field id of pivot table.
     */
    tableFieldId: string;
    /**
     * @property {string[]} items  The items to set the filter. Those items will be checked in the pivot table.
     */
    items: string[];
    /**
     * @property {boolean} isAll  The flag to set all items. True means all items, false means only the items in the items array.
     */
    isAll?: boolean;
}
export declare const SetPivotFilterCommand: ICommand<ISetPivotFilterCommandParams>;

import { Injector, Disposable } from '@univerjs/core';
import { IDocumentOffsetConfig } from '@univerjs/engine-render';
export declare class DocPrintPageCanvasView extends Disposable {
    private readonly _injector;
    private readonly _page;
    private readonly _unitId;
    private readonly _offsetConfig;
    private _container;
    private _root;
    private _engine;
    private _scene;
    private _key;
    private _documents;
    private _viewMain;
    private _effects;
    get container(): HTMLDivElement;
    get root(): HTMLDivElement;
    private get _currentRender();
    private get _skeleton();
    private get _skeletonData();
    private get _paperSize();
    constructor(_injector: Injector, _page: number, _unitId: string, _offsetConfig: IDocumentOffsetConfig);
    private _calculateLayout;
    private _initRenderer;
    private _addComponent;
    private _addViewport;
    prepare(): void;
    render(): void;
}

import { LocaleService } from '@univerjs/core';
import { OptionType } from './Options';
export declare function useTranslatedOptions(localeService: LocaleService, options: OptionType[]): {
    label: string;
    value: string;
}[];

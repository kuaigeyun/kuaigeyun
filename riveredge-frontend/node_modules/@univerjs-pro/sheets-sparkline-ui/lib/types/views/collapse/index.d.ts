import { CSSProperties, ReactNode } from 'react';
interface ICollapseProps {
    title: string;
    children: ReactNode;
    containerStyle?: CSSProperties;
}
export default function Collapse({ title, children, containerStyle }: ICollapseProps): import("react/jsx-runtime").JSX.Element;
export {};

import { IRange, IScale } from '@univerjs/core';
import { SpreadsheetSkeleton, UniverRenderingContext, SheetExtension } from '@univerjs/engine-render';
import { IPrintGridService } from '../../services/print-grid.service';
import { ISheetPrintManagerService } from '../../services/sheet-print-manager.service';
export declare class PrintGridExtension extends SheetExtension {
    uKey: string;
    Z_INDEX: number;
    static printGridService: IPrintGridService | null;
    static printManagerService: ISheetPrintManagerService | null;
    draw(ctx: UniverRenderingContext, parentScale: IScale, skeleton: SpreadsheetSkeleton, _diffRanges: IRange[]): void;
    private _drawPrintGrid;
}

declare function generateKey(): string;
declare function encrypt(plaintext: string, key: string): string;
declare function decrypt(ciphertext: string, key: string): string;
export declare const UniverAES: {
    generateKey: typeof generateKey;
    encrypt: typeof encrypt;
    decrypt: typeof decrypt;
};
export {};

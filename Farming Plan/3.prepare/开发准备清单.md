# RiverEdge SaaS 多租户框架 - 开发准备清单

> **目标**：完成所有准备工作，确保可以开始 Week 1 开发任务

## ✅ 环境检查

- [ ] **Python 3.13+** 已安装
  ```bash
  python --version  # 应显示 Python 3.13.x 或更高
  ```

- [ ] **Node.js 20+** 已安装
  ```bash
  node --version  # 应显示 v20.x.x 或更高
  ```

- [ ] **PostgreSQL 15+** 已安装并运行
  ```bash
  psql --version  # 应显示 15.x 或更高
  # 验证服务运行：psql -U postgres -c "SELECT version();"
  ```

- [ ] **Redis 7.2+** 已安装并运行
  ```bash
  redis-cli --version  # 应显示 7.2.x 或更高
  redis-cli ping  # 应返回 PONG
  ```

- [ ] **Git** 已安装
  ```bash
  git --version
  ```

- [ ] **pnpm** 已安装（推荐）
  ```bash
  npm install -g pnpm
  pnpm --version
  ```

## ✅ 项目初始化

- [ ] **创建项目目录结构**
  ```bash
  # 确保在项目根目录 F:\dev\riveredge
  mkdir riveredge-core
  mkdir riveredge-shell
  mkdir riveredge-seeds
  ```

- [ ] **创建后端虚拟环境**
  ```bash
  cd riveredge-core
  python -m venv venv

  # Windows 激活
  venv\Scripts\activate

  # macOS/Linux 激活
  source venv/bin/activate
  ```

- [ ] **升级 pip**
  ```bash
  pip install --upgrade pip
  ```

- [ ] **创建后端项目目录结构**
  ```bash
  mkdir -p src/app src/api/v1 src/core src/models src/schemas src/services src/utils
  mkdir -p migrations tests logs
  ```

## ✅ 后端依赖安装

- [ ] **创建 requirements.txt**
  ```bash
  cd riveredge-core
  # 创建文件并添加依赖（见下方模板）
  ```

- [ ] **创建 requirements-dev.txt**
  ```bash
  # 创建文件并添加开发依赖（见下方模板）
  ```

- [ ] **安装后端依赖**
  ```bash
  pip install -r requirements.txt
  pip install -r requirements-dev.txt
  ```

- [ ] **验证依赖安装**
  ```bash
  python -c "import fastapi; print('FastAPI:', fastapi.__version__)"
  python -c "import tortoise; print('Tortoise ORM:', tortoise.__version__)"
  ```

## ✅ 后端配置文件

- [ ] **创建 .env.example 文件**
  ```bash
  # 创建文件并添加配置模板（见下方模板）
  ```

- [ ] **创建 .env 文件**
  ```bash
  cp .env.example .env
  # 编辑 .env 文件，填入实际配置值（数据库密码、密钥等）
  ```

- [ ] **创建 .gitignore 文件**
  ```bash
  # 创建文件并添加忽略规则（见下方模板）
  ```

- [ ] **创建 pyproject.toml（可选）**
  ```bash
  # 创建文件并添加代码规范配置（见下方模板）
  ```

## ✅ 前端项目初始化

- [ ] **使用 Umi 创建前端项目**
  ```bash
  cd riveredge-shell
  npm create umi@latest .
  # 选择配置：
  # - @umijs/max（包含所有插件）
  # - TypeScript
  # - Ant Design Pro 模板
  ```

- [ ] **安装前端依赖**
  ```bash
  pnpm install
  ```

- [ ] **验证前端项目**
  ```bash
  pnpm dev  # 应能启动开发服务器
  ```

## ✅ 最终验证

- [ ] **验证 Python 环境**
  ```bash
  python --version  # Python 3.13+
  pip --version
  ```

- [ ] **验证虚拟环境**
  ```bash
  cd riveredge-core
  # 激活虚拟环境后
  which python  # 应指向 venv 中的 Python
  ```

- [ ] **验证后端依赖**
  ```bash
  python -c "import fastapi, tortoise, pydantic; print('依赖正常')"
  ```

- [ ] **验证数据库连接**
  ```bash
  psql -U postgres -d riveredge_dev -c "SELECT version();"
  ```

- [ ] **验证 Redis 连接**
  ```bash
  redis-cli ping  # 应返回 PONG
  ```

- [ ] **验证 Node.js 环境**
  ```bash
  node --version  # v20.x.x 或更高
  pnpm --version
  ```

- [ ] **验证前端项目**
  ```bash
  cd riveredge-shell
  pnpm tsc --noEmit  # 应无错误
  ```

## 📋 配置文件模板

### requirements.txt

```txt
# 核心框架
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.9.0
pydantic-settings>=2.5.0

# ORM 和数据库
tortoise-orm>=0.20.0
aerich>=0.7.0
asyncpg>=0.29.0

# 缓存
redis>=5.0.0  # redis-py，使用 redis.asyncio 异步接口

# 认证和安全
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4

# HTTP 客户端
httpx>=0.27.0

# 日志
loguru>=0.7.0
```

### requirements-dev.txt

```txt
# 包含生产依赖
-r requirements.txt

# 测试框架
pytest>=8.0.0
pytest-asyncio>=0.23.0
pytest-cov>=4.1.0

# 代码质量
black>=24.0.0
ruff>=0.1.0
mypy>=1.7.0

# 开发工具
ipython>=8.18.0
```

### .env.example

```env
# 应用配置
APP_NAME=RiverEdge SaaS Framework
APP_VERSION=1.0.0
DEBUG=True
ENVIRONMENT=development

# 安全配置
SECRET_KEY=your-secret-key-here-change-in-production-min-32-chars
# 生成方式：python -c "import secrets; print(secrets.token_urlsafe(32))"

# 数据库配置（PostgreSQL）
DATABASE_URL=postgresql://postgres:password@localhost:5432/riveredge_dev
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=your-password-here
DB_NAME=riveredge_dev

# Redis 配置
REDIS_URL=redis://localhost:6379/0
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_DB=0

# JWT 配置
JWT_SECRET_KEY=your-jwt-secret-key-here-change-in-production-min-32-chars
JWT_ALGORITHM=HS256
JWT_ACCESS_TOKEN_EXPIRE_MINUTES=30
JWT_REFRESH_TOKEN_EXPIRE_DAYS=7

# CORS 配置
CORS_ORIGINS=http://localhost:3000,http://localhost:8000
```

### .gitignore

```gitignore
# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
venv/
env/
ENV/
.venv

# 环境变量
.env
.env.local

# IDE
.vscode/
.idea/
*.swp
*.swo

# 数据库
*.db
*.sqlite

# 日志
*.log

# 测试
.pytest_cache/
.coverage
htmlcov/

# 迁移
migrations/versions/
```

### pyproject.toml（可选）

```toml
[tool.black]
line-length = 100
target-version = ['py313']

[tool.ruff]
line-length = 100
target-version = "py313"

[tool.mypy]
python_version = "3.13"
warn_return_any = true
warn_unused_configs = true
```

## 🚀 完成准备后

完成所有检查项后，可以开始 **Week 1** 的开发任务：

1. **后端核心框架搭建**（Week 1 Day 1-2）
   - 创建 FastAPI 应用入口
   - 配置数据库连接
   - 配置 Redis 缓存
   - 实现 BaseModel（包含 tenant_id）

详细开发步骤请参考：[4.框架开发计划.md](../1.plan/4.框架开发计划.md)

---

**最后更新**：2025-01-XX
